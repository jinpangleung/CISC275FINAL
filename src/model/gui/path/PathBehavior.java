package model.gui.path;

/**
 * PathBehavior must have a method that decides what to do when the path is at its destination
 * 
 * @author Eric
 *
 */

public interface PathBehavior {
	
	public void terminate(Path p);

}
